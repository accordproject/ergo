# The Vim plugin for Ergo

Install this plugin via Pathogen by moving this directory from wherever you
found it to the directory `~/.vim/bundle/ergo.vim` (and ensuring that
`pathogen#infect()` is in your `~/.vimrc` file).

